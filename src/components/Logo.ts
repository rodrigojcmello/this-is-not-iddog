import styled from '../assets/styled-components';

const Logo = styled.h2`
  font-size: 24px;
  font-weight: 400;
  margin-bottom: 64px;
  margin-top: 64px;
  text-align: center;
`;

export default Logo;
